const initialState = {
  temperature: null,
  icon: null,
  description: null,
  feelsLike: null,
  wind: null,
  visibility: null,
}

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state
  }
}
